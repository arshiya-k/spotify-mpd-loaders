[project]
name = "spotify-mpd-loaders"
version = "0.1.0"
description = "Benchmarking ways to load the Spotify Million Playlist Dataset into Postgres"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
]

[tool.setuptools]
packages = ["common"]
